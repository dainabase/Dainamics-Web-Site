import { useState } from 'react';

interface ShareButtonsProps {
  title: string;
  url?: string;
}

const ShareButtons = ({ title, url }: ShareButtonsProps) => {
  return <div>Share buttons temporarily disabled</div>;
};

export default ShareButtons;
